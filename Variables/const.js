/*
    if need some constant value in our program,
    then we can declare a variable with const keyword.
    It restrict the value changing property.
*/

const pi = 3.1416;

var radius = 4;

var area = pi * radius * radius;

console.log("The area of the circle is " + area);